services:
  app:
    build: .
    ports:
      - "8004:8000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      - NINER_ROUTER_URL=http://host.docker.internal:20128/v1
